#include <stdio.h>
#include <stdint.h>

/// Idea:
/// a + b + c = 1000
/// a^2 + b^2 = c^2
/// a < b < c
/// => c = sqrt(a^2 + b^2)
/// => a + b + sqrt(a^2 + b^2) = 1000
/// => ...

void solve()
{

    fprintf(stdout, "triplet not found.\n");
}

int main()
{
    solve();
    return 0;
}
